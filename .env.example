APIFY_API_TOKEN=your_apify_api_token
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key
RESEND_API_KEY=your_resend_api_key
RECIPIENT_EMAIL=your_recipient_email@example.com

# Optional: Comma-separated list of keywords to search for posts (defaults provided in code)
SEARCH_KEYWORDS="chatbot hallucinating, AI agent misbehaving, chatbot fail"

# Optional: Custom From email for Resend (must be verified in Resend)
FROM_EMAIL="Daily Briefing <onboarding@resend.dev>"
